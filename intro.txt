numbers = list(range(50,60))
numbers


def isPrime(n):
    if n > 1:
        for i in range(2,n):
            if(n%i==0):
                return False
        return True
def get_prime_numbers(N):
    primes = []
    num = 2
    while len(primes) < N:
        if is_prime(num):
            primes.append(num)
        num += 1
    return primes